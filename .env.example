# --- Client-safe (bundled into the Vite build; never put secrets here) ---
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# --- Email provider (optional: wire into an invitation-delivery function) ---
# EMAIL_PROVIDER=
# EMAIL_PROVIDER_API_KEY=
# EMAIL_FROM_ADDRESS=

# Never store real care data, real user IDs, or production secrets here.
